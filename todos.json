{
  "_doc": "GROUND TRUTH for agent work in this repo. Machine-first: written to be read and updated by AI agents. Human-readable detail lives in docs/, ISSUES.md and artifacts/ and is only referenced here (refs/issue), never duplicated. Rules for using this file: AGENTS.md §TODO workflow.",
  "version": 1,
  "updated_at": "2026-09-24T13:07Z",
  "status_legend": {
    "open": "not started",
    "in_progress": "started, not finished",
    "done": "finished AND verified — kept forever for traceability, never deleted or renumbered",
    "blocked": "cannot proceed; see block_on",
    "dropped": "decided against; see reason"
  },
  "resume": {
    "next_todo": "T16",
    "state": "24.09: T15 DONE — fb_stub.go (!linux dev) final state: display(in) = ReadFile → pngDecode → toGray → writePGM (P5) to /tmp/dash-out.pgm (DASH_FB override kept). T15 cleanup: hand-written itoa → strconv.Itoa (dead DASH_FLIPX/Y branches were already dropped with the mmap path in T14). Bugfix found while verifying: P5 maxval was written as a raw 0xFF byte (no parser accepts that — PIL rejects it) → now ASCII '255' (17 B header + w×h raster). Verified: gofmt clean, go build/vet/test -count=1 green (darwin), linux/arm GOARM=7 cross-compile OK, DASH_FB smoke → 1,552,273 B PGM that PIL opens (1072,1448, mode L). UNVERIFIED on device (T17): eips exec rc, diag.log, user display check (visible / t180 orientation / flicker). On card (unchanged): refresh.sh v4 pre-T14 (2,720 B) + dash (mmap, INVISIBLE) running; visible = bistable dashboard.png. T19/T18 still blocked on the user's n8n API key (independent).",
    "how_to_resume": "Work exactly one todo at a time, respecting depends_on. Next = T16 (resolve newest go1.23.x patch — e.g. `go list -m -versions golang.org/toolchain` via proxy; GOTOOLCHAIN=go1.23.x CGO_ENABLED=0 GOOS=linux GOARCH=arm GOARM=7 go build -trimpath -ldflags=-s; verify static ARM via file + `go version <bin>`, no /Users in strings, size < ~7 MB vs T13 baseline 6,357,152 B; replace artifacts/binaries/dash + docs/05 SHA + todos.json in one commit). Then T17 (announce card write; cp new dash + updated refresh.sh → /Volumes/Kindle; hot re-stage on size change, no reboot needed; STOP and request user verification: visible? t180 orientation? flicker? + readbacks /mnt/us/diag.log (eips rc) and /mnt/us/refresh.log (dl ok / render rc)). Fallback if eips rc≠0 persists: dash write-PNG-only + refresh.sh calls /usr/sbin/eips itself (docs/03 fallback). After T17 success: update README current-state + docs (repo wins over Notion). After every state change: update this resume block + updated_at, commit together with the work."
  },
  "todos": [
    {
      "id": "T01",
      "title": "Jailbreak Kindle Voyage (WatchThis legacy + custom hotfix bin)",
      "status": "done",
      "issue": "I4",
      "refs": ["docs/02-jailbreak.md", "artifacts/jailbreak/"],
      "details": "WatchThis release tree + custom hotfix (SP01, SHA 2102e30f…) installed 21.09.2026. ROM stays read-only; exec from FAT not possible (drives T04). No 5.14+ update (breaks chain).",
      "done_at": "2026-09-21",
      "verification": "hotfix bin on card, .boot + refresh.sh run after reboot, capability est. 100% (MRPI logs)"
    },
    {
      "id": "T02",
      "title": "Network PoC: device reaches n8n webhook (wget)",
      "status": "done",
      "issue": "I4",
      "refs": ["artifacts/logs/http-test.log", "docs/04-n8n-integration.md"],
      "details": "BusyBox wget from device to the n8n webhook over LAN; binary response readable on card.",
      "done_at": "2026-09-21",
      "verification": "wget PoC green, HTTP 200 + PNG bytes on card"
    },
    {
      "id": "T03",
      "title": "Static ARM Go binary dash (cross-compiled, CGO off)",
      "status": "done",
      "issue": "I1",
      "refs": ["artifacts/binaries/dash", "docs/01-hardware.md"],
      "details": "Static linux/arm binary, runs on kernel 3.0.35. Source on the user's machine (canonical repo-relative location: `src/kindle-dash/`), not in repo yet — see T11.",
      "done_at": "2026-09-22",
      "verification": "deployed + executed on device (SHA e2732446…, 6,291,616 B)"
    },
    {
      "id": "T04",
      "title": "Exec-from-FAT problem: stage to /tmp, re-stage on size change",
      "status": "done",
      "issue": "I1",
      "refs": ["artifacts/refresh.sh"],
      "details": "refresh.sh v4: size check → re-stage dash to /tmp (tmpfs) → run. Makes future dash replacements hot-swappable without reboot choreography.",
      "done_at": "2026-09-24",
      "verification": "refresh.sh v4 (2,720 B, SHA b1c06190…) live on card, cycles clean in refresh.log"
    },
    {
      "id": "T05",
      "title": "Identify eips -g as render path + prove the two hard requirements",
      "status": "done",
      "issue": "I5",
      "refs": ["docs/03-eink-rendering.md", "artifacts/images/"],
      "details": "eips -b = NO-OP on KV. eips -g: (1) IHDR color type MUST be 0 (grayscale), (2) exactly 1 IDAT (first chunk read, rest dropped → zoomed crop). Supersedes the afternoon 'accepts 8-bit RGB' belief via controlled 32×32 experiment.",
      "done_at": "2026-09-23",
      "verification": "controlled 8×8/32×32 + 3,697 B full-frame tests pixel-exact; broken cases preserved (dashboard-real.png.bak, webhook captures)"
    },
    {
      "id": "T06",
      "title": "v1 unattended refresh loop (wget + eips, ~1 min cycle)",
      "status": "done",
      "issue": "I3",
      "refs": ["artifacts/refresh.sh.bak-v1"],
      "details": "First live loop; worked unattended. Superseded by v3/v4 (kept for history).",
      "done_at": "2026-09-22",
      "verification": "ran unattended on device (superseded, not a failure)"
    },
    {
      "id": "T07",
      "title": "n8n code node t180 (gold code, sandbox-verified byte-exact)",
      "status": "done",
      "issue": "I2",
      "refs": ["n8n/code/t180-n8n.js", "docs/04-n8n-integration.md", "artifacts/images/golden_t180.png"],
      "details": "t180-n8n.js (14,565 B, SHA 76f8f8b2…) implements transpose+180° in the code node; verified byte-exact vs golden in the n8n sandbox. NEVER deployed (API key 401 → T19). Orientation therefore still handled on device side.",
      "done_at": "2026-09-23",
      "verification": "28,052,160-pixel comparison PASS in sandbox; deploy blocked (T19)"
    },
    {
      "id": "T08",
      "title": "Working-buffer analysis (fb0 dump, plane/stride, 16 px drift)",
      "status": "done",
      "issue": "I8",
      "refs": ["docs/03-eink-rendering.md"],
      "details": "fb0 = 2 planes (3,104,512 B = 1072×2896, 16-bit LE); pre-FLASH dump: 4 values {0x3de, 0x3c0, 0x1e, 0x0}; agreement ~0.86; ~16 px/row drift (1072 vs 1088 stride); shear simulation gives no improvement → NO shear in production path.",
      "done_at": "2026-09-23",
      "verification": "eips -k dump analysis (documented in docs/03); decision: go through eips, no offset hacks"
    },
    {
      "id": "T09",
      "title": "v3 Mac-proxy architecture + bistable dashboard.png visible",
      "status": "done",
      "issue": "I9",
      "refs": ["artifacts/refresh.sh.bak-v3", "artifacts/dashboard.png"],
      "details": "Mac proxy served the image; eips -g rendered it each cycle. Kept the Mac in the loop permanently → superseded by final architecture (T12-T17). Currently visible image is the bistable remainder of this phase.",
      "done_at": "2026-09-23",
      "verification": "user-visible dashboard (superseded by design, not by failure)"
    },
    {
      "id": "T10",
      "title": "Final architecture decision: device-side grayscale + single-IDAT + eips",
      "status": "done",
      "issue": "I10",
      "refs": ["ISSUES.md", "docs/03-eink-rendering.md"],
      "details": "dash = fetch + Go decode + Floyd-Steinberg + saveKindlePNG → /mnt/us/dashboard.png; display = exec eips -g (30 s), download 300 s. n8n stays RGB (converted on device).",
      "done_at": "2026-09-24",
      "verification": "decided 24.09 after T08 working-buffer analysis; plan fixed at I10 steps 1-7"
    },
    {
      "id": "T11",
      "title": "Obtain Go source into src/kindle-dash/ (repo-relative)",
      "status": "done",
      "issue": "I10",
      "done_at": "2026-09-24",
      "verification": "All 6 files present in src/kindle-dash/ and byte-match the Notion Artifacts canonical sizes (go.mod 20, main.go 2695, render.go 416, convert.go 2604, fb_linux.go 2322, fb_stub.go 511). go vet clean; compiles for darwin (local) + linux/arm GOARM=7 (device arch); the linux/arm build is 6,291,616 B = the EXACT size of the deployed reference binary. convert.go 1-byte loss at the Teil A/B join found + fixed (2603→2604, blank line before errA restored per Notion Teil B). A fresh build's SHA differs from the reference only because it was built with go1.27.1 and no -trimpath — T16 rebuilds with Go ≤1.23 + -trimpath. Gold-standard confirmation = byte-diff vs the user's Mac originals (Notion storage is lossy: trailing newlines stripped).",
      "refs": ["AGENTS.md (actual state, bullet 4)", "artifacts/binaries/dash (reference binary)", "Notion Artifacts page (canonical source)"],
      "details": "Files: go.mod, main.go, render.go, convert.go, fb_linux.go, fb_stub.go (flat layout, no cmd/dash/), transcribed from the Notion Artifacts page (the canonical source; split code blocks rejoined). NOTE: files have NO trailing newline — faithful to the Notion code blocks, which strip the trailing \\n; `gofmt -l` flags all 5 .go files for that single point only (mid-file content is gofmt-clean). Kept faithful (no `gofmt -w`) per the 'must not be invented' rule + documented sizes; gofmt-cleaning (add trailing newlines) is an optional user decision. 24.09: absolute user-specific paths removed from all repo references (user works across machines); canonical location is repo-relative src/kindle-dash/."
    },
    {
      "id": "T12",
      "title": "Implement saveKindlePNG in convert.go (filter-0 rows → ONE zlib stream → 1 IDAT, color type 0)",
      "status": "done",
      "issue": "I10-1",
      "depends_on": ["T11"],
      "done_at": "2026-09-24",
      "refs": ["docs/03-eink-rendering.md (Python reference = the spec)", "AGENTS.md (Definition of Done 1)", "src/kindle-dash/convert_test.go"],
      "details": "saveKindlePNG + writePNGChunk added to convert.go: manual PNG (SIG + IHDR bd=8 ct=0 compression/filter/interlace=0, one IDAT via compress/zlib over all filter-0 rows, CRC32 IEEE), NOT image/png (blocks at ~32 KB → 48 IDAT). Self-test in convert_test.go: (a) 64×32 gradient — chunk walk asserts exactly 1 IDAT, bitdepth 8, colortype 0, correct dims, and a pixel-exact round-trip via *image.Gray; (b) 1072×1448 sample dump for external validation.",
      "verification": "go vet clean; go test PASS; darwin build compiles; linux/arm GOARM=7 build = 6,291,616 B = the EXACT deployed-reference size (saveKindlePNG is dead code until T13 wires it in → linker eliminates it, binary size unchanged). docs/03 shell validator on the 1072×1448 sample: size 1072×1448, bitdepth 8, colortype 0, 1 IDAT, raw scanline bytes 1,553,704 = 1448×(1072+1). Python reference golden (save_kindle_png on identical input): Go-decompressed raw scanlines are BYTE-IDENTICAL to the reference (full file differs only in the zlib stream, which eips never reads raw)."
    },
    {
      "id": "T13",
      "title": "main.go get(): remove all rotation/mirror code (image arrives pre-oriented from n8n)",
      "status": "done",
      "issue": "I10-2",
      "depends_on": ["T11"],
      "refs": ["AGENTS.md (Definition of Done 2)", "docs/03-eink-rendering.md", "src/kindle-dash/main_test.go"],
      "details": "get() now wires the fetched+decoded image through toGray (device-side Floyd-Steinberg 8-bit) → kindlePNG (the T12 single-IDAT builder) → atomic write to <out>.png, then render() for immediate display. NO rotation/mirror is applied in get() (image arrives pre-oriented from n8n; the remaining device-side transpose/DASH_FLIPX/Y live in fb_linux.go writeFB and are dropped with the mmap path in T14/T15). convert.go: saveKindlePNG refactored to delegate to a new kindlePNG(w,h,gray) []byte builder (keeps the len check + os.WriteFile) so get() and saveKindlePNG share one PNG path. Added skip-if-unchanged (bytes.Equal on the generated bytes) + atomic tmp+rename. Output geometry stays 1072×1448.",
      "done_at": "2026-09-24",
      "verification": "go vet clean; go test PASS (incl. new TestGet: httptest-served 1072×1448 RGB → get() → single-IDAT / colortype 0 / bitdepth 8 / correct dims / pixel-exact vs toGray). docs/03 shell validator on the 1072×1448 sample: 1 IDAT, bd8, ct0, interlace 0, 1,553,704 raw scanline bytes; decompressed scanlines byte-identical to the Python save_kindle_png reference (full file differs only in the zlib stream — Go's compress/zlib vs C zlib — which eips never reads raw). linux/arm GOARM=7 build now 6,357,152 B (was 6,291,616 B; kindlePNG no longer dead code → +65,536 B). Output stays 1072×1448, no device-side orientation."
    },
    {
      "id": "T14",
      "title": "fb_linux.go display(): replace mmap with exec /usr/sbin/eips -g <file> -x 0 -y 0",
      "status": "done",
      "issue": "I10-3",
      "depends_on": ["T11"],
      "refs": ["docs/03-eink-rendering.md (eips flag table + open risks)", "AGENTS.md (Definition of Done 3)"],
      "details": "fb_linux.go: display(in string) = exec.CommandContext (30 s) of /usr/sbin/eips -g <in> -x 0 -y 0; rc (0 / *exec.ExitError code / -1 on launch error or timeout) + duration ALWAYS appended to /mnt/us/diag.log via new best-effort diagf() (UTC, failed writes dropped). Removed with the mmap path: writeFB, DASH_FLIPX/DASH_FLIPY env reads, and the dead byte-buffer transpose in convert.go. render.go: render(in) is now a thin display(in) wrapper (committed pngDecode kept). main.go: get() no longer calls render() — get = write-only (steps 1-3 of the docs/03 diagram), render = the one EPDC wave per cycle (driven by refresh.sh, call pattern byte-stable); CLI render takes exactly 1 arg (old [fbpath] gone). fb_stub.go (non-linux dev): display → P5 PGM to /tmp/dash-out.pgm (DASH_FB override kept). artifacts/refresh.sh: comments-only update (2,599 B, SHA d8d84e99…, was 2,720 B / b1c06190…). RISK (open until T17): static binary may lack permission to exec eips (AppArmor/seccomp on ROM); rc is logged to diag.log either way; fallback if rc!=0 persists = dash stops at write-PNG-only and refresh.sh itself calls eips (docs/03).",
      "done_at": "2026-09-24",
      "verification": "Local only (darwin): gofmt clean; go build ./... + go vet + go test PASS; linux/arm GOARM=7 cross-compile OK (8.8 MB unstripped under local go1.27 — T16 rebuilds with Go ≤1.23 + -trimpath -ldflags=-s); dev-stub smoke: DASH_FB override → valid P5 PGM, pixel-checked. UNVERIFIED on device (T17): eips exec rc, diag.log contents, user display check (visible / t180 orientation / flicker)."
    },
    {
      "id": "T15",
      "title": "fb_stub.go: PGM output for development on the Mac",
      "status": "done",
      "issue": "I10-4",
      "depends_on": ["T11"],
      "refs": ["AGENTS.md (Definition of Done 4 context)"],
      "details": "fb_stub.go (!linux) final state: display(in) = ReadFile → pngDecode → toGray → writePGM (P5) to /tmp/dash-out.pgm (DASH_FB override kept). T15 cleanup: hand-written itoa → strconv.Itoa (the dead DASH_FLIPX/Y branches were already removed with the mmap path in T14). Bugfix found during verification: the P5 maxval was written as a raw 0xFF byte (no standard parser accepts that — PIL rejects it) → now ASCII '2','5','5' (17 B header + w×h raster).",
      "done_at": "2026-09-24",
      "verification": "gofmt clean; go build ./... + go vet + go test -count=1 PASS (darwin); linux/arm GOARM=7 cross-compile OK. DASH_FB smoke: dash render artifacts/dashboard.png → 1,552,273 B PGM (17 B header + 1,552,256 = 1072×1448 raster); PIL opens it (1072,1448, mode L)."
    },
    {
      "id": "T16",
      "title": "Static ARM build (Go ≤ 1.23) + size check",
      "status": "open",
      "issue": "I10-5",
      "depends_on": ["T12", "T13", "T14", "T15"],
      "refs": ["AGENTS.md (hard technical rules, Go cap)", "artifacts/binaries/dash"],
      "details": "CGO_ENABLED=0 GOOS=linux GOARCH=arm GOARM=7 go build -ldflags=\"-s\". Go 1.24+ FORBIDDEN (needs Linux ≥3.2; device 3.0.35). Compare size to 6.29 MB; on success replace artifacts/binaries/dash + update SHA in docs/05."
    },
    {
      "id": "T17",
      "title": "Deploy + user verification (visibility, orientation, flicker) + log check",
      "status": "open",
      "issue": "I10-6",
      "depends_on": ["T16"],
      "refs": ["docs/03-eink-rendering.md (validation shell + orientation references)", "artifacts/images/orient-test-*.png", "artifacts/refresh.sh"],
      "details": "Deploy via hot re-stage (refresh.sh v4 size check) + reboot; validate_png on the produced file; user confirms: visible? orientation vs t180 reference? flicker acceptable? Then: refresh.log shows 'dl ok' + 'render rc=0'. On success: update README current-state table + docs (repo wins over Notion)."
    },
    {
      "id": "T18",
      "title": "Optional: n8n grayscale code node (replace t180, deliver 1-IDAT grayscale)",
      "status": "open",
      "issue": "I10-7",
      "depends_on": ["T19"],
      "block_on": "Code nodes do not execute on this n8n instance (execution error, 0 runs) AND API key 401 — change would have to be made in the UI by the user.",
      "refs": ["docs/04-n8n-integration.md (open items)"],
      "details": "Relieves dash (input already grayscale + 1 IDAT). Only after T17 proves the device path, if still wanted."
    },
    {
      "id": "T19",
      "title": "Fix n8n API key (401) to allow workflow changes via API",
      "status": "blocked",
      "issue": "I6",
      "block_on": "User must provide/rotate a working API key in the n8n instance; everything else (sanitizing procedure, target nodes) is documented in docs/04.",
      "refs": ["docs/04-n8n-integration.md"],
      "details": "Unblocks T18 and any future workflow change without the UI."
    }
  ]
}
